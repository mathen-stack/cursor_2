"""
LinkedIn collection workflow (FSM / navigation engine).

Responsibilities (to be implemented later):
- Execute the production loop:
  locate window → scan jobs → open job → wait detail → extract JD →
  save → next job → paginate → complete.
- Call vision agent + automation + linkedin detectors + storage.
- Enforce retries, action budgets, and need-user escalation.
"""

# Intentionally unimplemented — structure only.
