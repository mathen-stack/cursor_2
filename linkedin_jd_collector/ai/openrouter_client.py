"""
OpenRouter API HTTP client.

Responsibilities (to be implemented later):
- Authenticate with OPENROUTER_API_KEY.
- Send multimodal chat/completions requests (text + screenshot images).
- Return raw model text/JSON for validation.
- Handle timeouts, retries, and API error surfaces.

Does not execute mouse/keyboard actions.
"""

# Intentionally unimplemented — structure only.
