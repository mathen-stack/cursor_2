"""
Prompt templates for state-specific vision requests.

Responsibilities (to be implemented later):
- System/developer prompts defining JSON command contract.
- Per-state templates: scan jobs, wait detail, extract JD, paginate, recover.
- Inject runtime context (processed job signatures, page index, last error).

Keeps prompt text separate from HTTP client logic.
"""

# Intentionally unimplemented — structure only.
