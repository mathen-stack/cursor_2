"""
LinkedIn browser window detector.

Responsibilities (to be implemented later):
- Find the browser window/tab surface that contains LinkedIn (via PyWinAuto + vision).
- Focus the correct window before capture/actuation.
- Report ambiguity (multiple candidates) back to the UI as need-user.
- Supply window bounds for screenshot cropping and coordinate mapping.
"""

# Intentionally unimplemented — structure only.
