"""
Main application window (PyQt6).

Responsibilities (to be implemented later):
- Host Start / Stop / Pause controls.
- Show collection progress (page, job count, FSM state).
- Embed log console and settings access.
- Wire UI actions to the agent controller via signals/slots.

Does not run automation logic on the UI thread.
"""

# Intentionally unimplemented — structure only.
