import TailorApp from "../components/tailor-app";

export const metadata = {
  title: "Tailor uploaded resumes | Resume Tailor",
  description:
    "Upload perfect base resumes, match them to a JD, and tailor with ATS-safe generation rules.",
};

export default function TailorPage() {
  return <TailorApp />;
}
